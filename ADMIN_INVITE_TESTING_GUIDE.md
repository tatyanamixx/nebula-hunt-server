# 🧪 Руководство по тестированию приглашений админов

## 📋 Обзор процесса

1. **Отправка приглашения** - админ создает приглашение для нового админа
2. **Получение приглашения** - новый админ получает ссылку с токеном
3. **Валидация токена** - проверка действительности приглашения
4. **Регистрация админа** - создание аккаунта с паролем и 2FA
5. **Активация в БД** - обновление статуса приглашения

## 🚀 Пошаговое тестирование

### Шаг 1: Проверка существующих админов

```bash
# Проверить какие админы есть в системе
node check-admins.js

# Проверить пароль конкретного админа
node check-admin-password.js tatyanamixx@gmail.com
```

### Шаг 2: Отправка приглашения

```bash
# Тестирование API отправки приглашения
node test-invite-api.js
```

**Или через веб-интерфейс:**

1. Откройте `http://localhost:3000/admin/settings`
2. Войдите как существующий админ
3. Введите email нового админа (например, `newadmin@example.com`)
4. Нажмите "Send Invite"

### Шаг 3: Проверка созданного приглашения

```bash
# Проверить детали приглашения по email
node check-invite-token.js newadmin@example.com

# Проверить статус приглашения
node check-invite-status.js newadmin@example.com
```

### Шаг 4: Тестирование валидации токена

```bash
# Тестирование API валидации и регистрации
node test-invite-validation.js
```

**Или через веб-интерфейс:**

1. Откройте ссылку: `http://localhost:3000/admin/register?token=<TOKEN>`
2. Заполните форму регистрации
3. Настройте 2FA

### Шаг 5: Проверка результата

```bash
# Проверить что админ создан
node check-admins.js

# Проверить что приглашение помечено как использованное
node check-invite-status.js newadmin@example.com
```

## 📧 Тестирование Email-уведомлений

### Предварительный просмотр email

```bash
# Просмотреть содержимое email без отправки
node preview-email-content.js
```

Этот скрипт покажет:

-   HTML и текстовую версию email
-   Ссылку для приглашения
-   Все детали email

### Тестирование отправки email

```bash
# Простой тест отправки email
node test-email-invite.js
```

### Полный тест процесса с email

```bash
# Полный тест: логин → отправка приглашения → email → валидация
node test-full-invite-flow.js
```

**Примечание:** В режиме разработки email не отправляется реально, а выводится в консоль.

### Проверка содержимого email

Email содержит:

-   ✅ Приветствие с именем получателя
-   ✅ Информацию о роли (ADMIN/SUPERVISOR)
-   ✅ Кнопку "Accept Invitation"
-   ✅ Ссылку для копирования
-   ✅ Информацию о сроке действия (7 дней)
-   ✅ Предупреждение о безопасности

## 🔧 Диагностические скрипты

### Проверка состояния БД

```bash
# Проверить все приглашения
node check-invites.js

# Проверить конкретное приглашение
node check-invite-token.js <email>

# Проверить статус приглашения и админа
node check-invite-status.js <email>

# Проверить админов в системе
node check-admins.js

# Проверить пароль админа
node check-admin-password.js <email>
```

### Тестирование Email

```bash
# Предварительный просмотр содержимого email
node preview-email-content.js

# Просмотр email без отправки (рекомендуется)
node test-email-preview-only.js

# Получение реального токена из БД
node get-real-invite-token.js

# Простое тестирование отправки email
node test-email-simple.js

# Тестирование отправки email через email-service
node test-email-invite.js

# Полный тест процесса приглашения с email
node test-full-invite-flow.js
```

### Тестирование API

```bash
# Тест подключения к серверу
node test-server-connection.js

# Тест API приглашений
node test-invite-api.js

# Тест валидации и регистрации
node test-invite-validation.js
```

### Исправление проблем

```bash
# Исправить статус приглашения (если нужно)
node fix-invite-status.js <email> <adminId>

# Диагностика проблем с обновлением
node debug-invite-update.js <email> <adminId>
```

## 📊 Ожидаемые результаты

### После отправки приглашения:

-   В таблице `admininvites` появляется новая запись
-   `used: false`, `usedAt: null`, `usedBy: null`
-   `expiresAt` = текущее время + 7 дней

### После регистрации админа:

-   В таблице `admins` появляется новая запись
-   В таблице `admininvites` обновляется запись:
    -   `used: true`
    -   `usedAt: текущее время`
    -   `usedBy: ID нового админа`

## 🐛 Частые проблемы и решения

### 1. "Invalid Date" в интерфейсе

**Причина:** Проблемы с сериализацией дат
**Решение:** Исправлено в `admin-service.js` - добавлен `toISOString()`

### 2. Приглашение не помечено как использованное

**Причина:** Ошибка в `registerAdmin` методе
**Решение:** Добавлены вызовы `markInviteAsUsed` и `setPasswordWithExpiry`

### 3. Неправильные статусы

**Причина:** Несоответствие между фронтендом и бэкендом
**Решение:** Унифицированы статусы: `ACCEPTED`, `EXPIRED`, `PENDING`

### 4. Проблемы с подключением к серверу

**Причина:** Неправильный порт или недоступность
**Решение:** Проверить что сервер запущен на порту 5000

## 🔐 Безопасность

-   Приглашения действительны 7 дней
-   Каждый токен уникален и одноразовый
-   2FA обязательна для всех админов
-   Пароли хешируются с bcrypt

## 📝 Логи

Все операции логируются в:

-   `logs/app.log` - общие логи приложения
-   Консоль сервера - для отладки

## 🎯 Критерии успешного тестирования

✅ Приглашение создается с правильными данными
✅ Токен валидируется корректно
✅ Регистрация создает админа с паролем и 2FA
✅ Приглашение помечается как использованное
✅ Статусы отображаются правильно в интерфейсе
✅ Даты отображаются корректно
✅ Все API endpoints работают
✅ Email отправляется с правильным содержимым
✅ Ссылка в email ведет на форму регистрации
✅ Форма регистрации работает с токеном из email

## 🚨 Если что-то не работает

1. Проверьте логи сервера
2. Запустите диагностические скрипты
3. Проверьте состояние БД
4. Убедитесь что все миграции выполнены
5. Проверьте переменные окружения
6. Проверьте настройки SMTP в переменных окружения
7. Убедитесь что FRONTEND_URL настроен правильно
