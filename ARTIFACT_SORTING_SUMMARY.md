# Artifact Sorting by baseChance - Summary

## ✅ Что было добавлено

### 1. Backend сортировка

-   ✅ Обновлен сервис `artifact-template-service.js`
-   ✅ Артефакты теперь сортируются по `baseChance` ASC по умолчанию
-   ✅ Добавлена вторичная сортировка по `slug` для стабильности

### 2. Frontend сортировка

-   ✅ Добавлены состояния для сортировки (`sortBy`, `sortOrder`)
-   ✅ Создана функция `sortTemplates()` для клиентской сортировки
-   ✅ Добавлена функция `handleSort()` для переключения сортировки
-   ✅ Добавлены кнопки сортировки в UI с индикаторами направления

### 3. Поддерживаемые поля сортировки

-   ✅ **Base Chance** - по шансу нахождения (по умолчанию)
-   ✅ **Name** - по названию артефакта
-   ✅ **Rarity** - по редкости (COMMON → LEGENDARY)

### 4. Тестирование

-   ✅ Создан тестовый скрипт `test-artifact-sorting.js`
-   ✅ Проверка сортировки ASC/DESC
-   ✅ Верификация корректности сортировки

## 🔄 Логика сортировки

### Backend (по умолчанию)

```javascript
order: [
	['baseChance', 'ASC'],
	['slug', 'ASC'],
];
```

### Frontend (динамическая)

```typescript
// Сортировка по baseChance
case 'baseChance':
    aValue = a.baseChance || 0;
    bValue = b.baseChance || 0;
    break;

// Сортировка по редкости
case 'rarity':
    const rarityOrder = { 'COMMON': 1, 'UNCOMMON': 2, 'RARE': 3, 'EPIC': 4, 'LEGENDARY': 5 };
    aValue = rarityOrder[a.rarity] || 0;
    bValue = rarityOrder[b.rarity] || 0;
    break;
```

## 🎯 Результат сортировки

### По baseChance (ASC) - от редких к частым:

1. **Legendary** (0.1%) - самые редкие
2. **Epic** (1%) - очень редкие
3. **Rare** (2%) - редкие
4. **Uncommon** (3-4%) - необычные
5. **Common** (5%) - обычные

### По редкости (ASC):

1. **COMMON** → **UNCOMMON** → **RARE** → **EPIC** → **LEGENDARY**

## 🎨 UI элементы

### Кнопки сортировки

-   **Base Chance** - с индикатором ↑↓
-   **Name** - с индикатором ↑↓
-   **Rarity** - с индикатором ↑↓

### Визуальные индикаторы

-   Активная кнопка: синий фон
-   Направление: стрелки ↑↓
-   Переключение: клик меняет направление

## 📁 Измененные файлы

### Backend

-   `service/artifact-template-service.js` - обновлена сортировка по умолчанию

### Frontend

-   `nebulahunt-webclient/src/components/ArtifactTemplatesTab.tsx` - добавлена сортировка

### Тесты

-   `test-artifact-sorting.js` - новый тестовый скрипт

## 🚀 Использование

1. **Автоматическая сортировка**: артефакты загружаются уже отсортированными
2. **Переключение сортировки**: клик по кнопкам сортировки
3. **Изменение направления**: повторный клик по той же кнопке
4. **Визуальная обратная связь**: индикаторы показывают текущую сортировку

## 🎉 Результат

Теперь артефакты автоматически сортируются по редкости (baseChance), что делает интерфейс более удобным для администраторов. Редкие артефакты отображаются в начале списка, а обычные - в конце.
