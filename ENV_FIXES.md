# Исправления загрузки переменных окружения

## 🔧 Проблемы, которые были исправлены

### 1. Неправильная загрузка .env в db.js

**Проблема**: Файл `db.js` не загружал `.env` файл и использовал неправильную логику для разных окружений.

**Исправление**:

-   Добавлен `require('dotenv').config()` в начало файла
-   Упрощена логика - теперь всегда используется конфигурация из `config/database.js`
-   Убрана дублирующая логика для production/development

### 2. Неправильные переменные в скриптах

**Проблема**: Скрипты `test-db-connection.js`, `run-migrations.js`, `check-tables.js` использовали общие переменные окружения вместо специфичных для окружения.

**Исправление**:

-   Все скрипты теперь используют конфигурацию из `config/database.js`
-   Правильно определяют окружение через `NODE_ENV`
-   Используют специфичные переменные для каждого окружения

### 3. Проблемы в тестах

**Проблема**: Файл `tests/setup.js` использовал общие переменные окружения.

**Исправление**:

-   Теперь использует конфигурацию из `config/database.js`
-   Правильно определяет окружение для тестов

## 📋 Новые команды для диагностики

### Проверка переменных окружения

```bash
npm run env:check
```

Эта команда проверяет:

-   Наличие файлов `.env` и `env.example`
-   Загрузку всех переменных окружения
-   Конфигурацию базы данных
-   Подключение к базе данных
-   Выводит рекомендации по исправлению проблем

### Проверка подключения к БД

```bash
node test-db-connection.js
```

Теперь показывает:

-   Окружение, для которого выполняется проверка
-   Правильные параметры подключения из конфигурации
-   Подробные сообщения об ошибках

## 🔍 Как проверить исправления

1. **Запустите проверку переменных окружения**:

    ```bash
    npm run env:check
    ```

2. **Проверьте подключение к базе данных**:

    ```bash
    node test-db-connection.js
    ```

3. **Запустите миграции**:

    ```bash
    npm run migrate
    ```

4. **Запустите сервер**:
    ```bash
    npm run dev
    ```

## 📁 Файлы, которые были изменены

-   `db.js` - исправлена загрузка .env и логика конфигурации
-   `test-db-connection.js` - использует правильную конфигурацию
-   `run-migrations.js` - использует правильную конфигурацию
-   `check-tables.js` - использует правильную конфигурацию
-   `tests/setup.js` - использует правильную конфигурацию
-   `scripts/check-env-loading.js` - новый скрипт диагностики
-   `setup-server.js` - добавлена проверка переменных окружения
-   `package.json` - добавлена команда `env:check`

## 🎯 Результат

Теперь все файлы правильно:

-   Загружают `.env` файл
-   Используют специфичные переменные для каждого окружения
-   Показывают понятные сообщения об ошибках
-   Предоставляют инструменты для диагностики проблем

## 🚀 Рекомендации

1. **Всегда используйте команду проверки**:

    ```bash
    npm run env:check
    ```

2. **Создавайте .env файл на основе env.example**:

    ```bash
    cp env.example .env
    ```

3. **Проверяйте подключение к БД перед запуском**:

    ```bash
    node test-db-connection.js
    ```

4. **Используйте правильные переменные для каждого окружения**:
    - `DB_HOST_DEV`, `DB_NAME_DEV` для development
    - `DB_HOST_TEST`, `DB_NAME_TEST` для test
    - `DB_HOST_PROD`, `DB_NAME_PROD` для production
