# Поддержка .env.local - Сводка изменений

## 🎯 Что добавлено

Поддержка файла `.env.local` для локальных настроек разработки с приоритетом над `.env`.

## 📁 Новые файлы

### 1. `env.local.example`

-   Пример файла локальных настроек
-   Содержит типичные настройки для разработки
-   Не попадает в git (в .gitignore)

### 2. `config/env-loader.js`

-   Утилита для загрузки переменных окружения
-   Поддерживает приоритет: `.env.local` > `.env` > системные переменные
-   Включает валидацию и диагностику

### 3. `ENV_LOCAL_GUIDE.md`

-   Подробное руководство по использованию .env.local
-   Примеры и рекомендации

## 🔧 Измененные файлы

### Основные файлы приложения

-   `index.js` - использует новую систему загрузки
-   `db.js` - использует новую систему загрузки
-   `config/database.js` - использует новую систему загрузки

### Скрипты

-   `test-db-connection.js` - использует новую систему загрузки
-   `run-migrations.js` - использует новую систему загрузки
-   `check-tables.js` - использует новую систему загрузки
-   `tests/setup.js` - использует новую систему загрузки
-   `scripts/check-env-loading.js` - обновлен для работы с .env.local

### Настройка

-   `setup-server.js` - создает .env.local автоматически
-   `package.json` - добавлены новые команды

## 🚀 Новые команды

```bash
# Создание .env.local
npm run env:local

# Проверка переменных окружения (обновлена)
npm run env:check

# Автоматическая настройка (обновлена)
npm run setup
```

## 📋 Приоритет загрузки переменных

1. **`.env.local`** (высший приоритет) - локальные настройки
2. **`.env`** - основные настройки проекта
3. **Системные переменные окружения** - системные настройки

## 🔍 Как проверить работу

### 1. Создание .env.local

```bash
npm run env:local
```

### 2. Настройка локальных переменных

```env
# В .env.local
PORT=3001
DB_NAME_DEV=nebulahunt_dev_local
JWT_ACCESS_SECRET=local_secret_key
```

### 3. Проверка загрузки

```bash
npm run env:check
```

### 4. Запуск сервера

```bash
npm run dev
```

## ✅ Преимущества

1. **Локальные настройки** - каждый разработчик может иметь свои настройки
2. **Безопасность** - .env.local не попадает в git
3. **Гибкость** - можно переопределить любую переменную
4. **Диагностика** - улучшенная проверка переменных окружения
5. **Автоматизация** - автоматическое создание файлов при настройке

## 📚 Документация

-   [Руководство по .env.local](ENV_LOCAL_GUIDE.md)
-   [Настройки сервера](SERVER_CONFIG.md)
-   [Быстрый запуск](QUICK_START.md)
