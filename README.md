# Nebulahant Server

Сервер для Telegram Mini App игры "Nebulahant" - космической стратегии с элементами idle-геймплея.

## 🚀 Описание

Nebulahant - это игра, где игроки управляют галактиками, развивают экономику, выполняют задачи и участвуют в событиях. Сервер обеспечивает всю backend логику игры, включая аутентификацию через Telegram Mini Apps, управление состоянием игроков, систему апгрейдов, событий и задач.

## 📋 Содержание

-   [Установка и настройка](#установка-и-настройка)
-   [Архитектура](#архитектура)
-   [API Документация](#api-документация)
-   [Модели данных](#модели-данных)
-   [Сервисы](#сервисы)
-   [Безопасность](#безопасность)
-   [Развертывание](#развертывание)
-   [Логирование](#логирование)
-   [Тестирование](#тестирование)

## 🛠 Установка и настройка

### Требования

-   Node.js 18+
-   PostgreSQL 12+
-   npm или yarn

### Установка

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd nebulahant-server
```

2. Установите зависимости:

```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

4. Настройте переменные окружения:

```env
# Server
PORT=5000
CLIENT_URL=https://your-client-url.com

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nebulahant
DB_USER=postgres
DB_PASSWORD=your_password

# JWT
JWT_ACCESS_SECRET=your_access_secret
JWT_REFRESH_SECRET=your_refresh_secret

# Telegram Mini Apps
BOT_TOKEN=your_bot_token
```

5. Создайте базу данных:

```sql
CREATE DATABASE nebulahant;
```

6. Запустите сервер:

```bash
# Разработка
npm run dev

# Продакшн
npm start
```

## 🏗 Архитектура

### Структура проекта

```
nebulahant-server/
├── config/                 # Конфигурации
├── controllers/           # Контроллеры API
├── dtos/                 # Data Transfer Objects
├── exceptions/           # Кастомные исключения
├── middlewares/          # Express middleware
├── models/              # Sequelize модели
├── routes/              # API маршруты
├── service/             # Бизнес-логика
├── docs/                # Документация
├── examples/            # Примеры запросов
├── logs/                # Логи приложения
├── index.js             # Точка входа
├── db.js                # Конфигурация БД
└── package.json
```

### Компоненты системы

-   **Express.js** - веб-фреймворк
-   **Sequelize** - ORM для PostgreSQL
-   **PostgreSQL** - основная база данных
-   **JWT** - аутентификация
-   **Telegram Mini Apps** - интеграция с Telegram
-   **Pino** - логирование

### Поток данных

1. **Запрос** → Express Router
2. **Middleware** → TMA Auth → Rate Limiting → Validation
3. **Controller** → Service Layer
4. **Service** → Database (Sequelize)
5. **Response** → Client

## 🔌 API Документация

### Базовый URL

```
https://your-server.com/api
```

### Аутентификация

Все запросы (кроме регистрации/логина) требуют аутентификации через Telegram Mini Apps.

### Основные эндпоинты

#### Аутентификация (`/auth`)

-   `POST /auth/registration` - Регистрация пользователя
-   `POST /auth/login` - Вход в систему
-   `POST /auth/logout` - Выход из системы
-   `GET /auth/refresh` - Обновление токенов
-   `GET /auth/friends` - Получение списка друзей
-   `GET /auth/system-user` - Получение системного пользователя

#### Административные функции (`/admin`)

-   `GET /admin/users` - Список пользователей
-   `POST /admin/block-user` - Блокировка пользователя
-   `POST /admin/unblock-user` - Разблокировка пользователя

#### Галактики (`/galaxy`)

-   `GET /galaxy` - Получение галактик пользователя
-   `POST /galaxy` - Создание новой галактики
-   `PUT /galaxy/:id` - Обновление галактики
-   `DELETE /galaxy/:id` - Удаление галактики

#### Состояние пользователя (`/state`)

-   `GET /state` - Получение состояния пользователя
-   `PUT /state` - Обновление состояния пользователя

#### История состояний (`/history`)

-   `GET /history` - Получение истории изменений состояния

#### Апгрейды (`/upgrades`)

-   `GET /upgrades` - Получение доступных апгрейдов
-   `POST /upgrades/purchase` - Покупка апгрейда
-   `GET /upgrades/tree` - Получение дерева апгрейдов

#### События (`/events`)

-   `GET /events` - Получение активных событий
-   `POST /events/trigger` - Принудительный запуск события

#### Задачи (`/tasks`)

-   `GET /tasks` - Получение доступных задач
-   `POST /tasks/complete` - Завершение задачи

## 🗄 Модели данных

### User

Основная модель пользователя.

```javascript
{
  id: BIGINT (primary key),
  username: STRING,
  referral: BIGINT,
  role: ENUM('USER', 'ADMIN', 'SYSTEM'),
  blocked: BOOLEAN
}
```

### UserState

Состояние пользователя в игре.

```javascript
{
  id: BIGINT (primary key),
  userId: BIGINT (foreign key),
  state: JSONB, // Игровое состояние
  chaosLevel: FLOAT,
  stabilityLevel: FLOAT,
  entropyVelocity: FLOAT,
  // ... множество других полей
}
```

### Galaxy

Галактики пользователя.

```javascript
{
  id: BIGINT (primary key),
  userId: BIGINT (foreign key),
  starMin: INTEGER,
  starCurrent: INTEGER,
  price: INTEGER,
  seed: STRING (unique),
  particleCount: INTEGER,
  galaxyProperties: JSONB
}
```

### UpgradeNode

Доступные апгрейды.

```javascript
{
  id: STRING (primary key),
  name: STRING,
  description: JSONB,
  maxLevel: INTEGER,
  basePrice: INTEGER,
  effectPerLevel: FLOAT,
  category: ENUM('production', 'economy', 'special', ...)
}
```

### Task

Игровые задачи.

```javascript
{
  id: STRING (primary key),
  title: JSONB,
  description: JSONB,
  reward: INTEGER,
  condition: JSONB,
  icon: STRING
}
```

### GameEvent

Игровые события.

```javascript
{
  id: STRING (primary key),
  name: STRING,
  description: JSONB,
  type: ENUM('RANDOM', 'PERIODIC', 'ONE_TIME', ...),
  triggerConfig: JSONB,
  effect: JSONB
}
```

## 🔧 Сервисы

### UserService

Управление пользователями, регистрация, аутентификация.

### StateService

Управление состоянием пользователя, обновление игровых параметров.

### GalaxyService

Создание и управление галактиками.

### UpgradeService

Система апгрейдов, дерево прогрессии.

### EventService

Игровые события, их триггеры и эффекты.

### TaskService

Система задач и достижений.

### TokenService

Управление JWT токенами.

### LoggerService

Централизованное логирование.

## 🔒 Безопасность

### Аутентификация

-   Telegram Mini Apps интеграция
-   JWT токены (access + refresh)
-   Автоматическое обновление токенов

### Авторизация

-   Роли: USER, ADMIN, SYSTEM
-   Middleware для проверки прав
-   Защита административных функций

### Rate Limiting

-   Ограничение запросов по IP
-   Настраиваемые лимиты для разных эндпоинтов

### Валидация данных

-   Express-validator для входящих данных
-   Валидация JSONB структур
-   Санитизация пользовательского ввода

## 🚀 Развертывание

### Docker (рекомендуется)

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
EXPOSE 5000
CMD ["npm", "start"]
```

### PM2

```bash
npm install -g pm2
pm2 start ecosystem.config.js
```

### Nginx конфигурация

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 📝 Логирование

### Конфигурация Pino

```javascript
// config/logger.config.js
module.exports = {
	level: process.env.LOG_LEVEL || 'info',
	transport: {
		target: 'pino-pretty',
		options: {
			colorize: true,
		},
	},
};
```

### Уровни логирования

-   `error` - Критические ошибки
-   `warn` - Предупреждения
-   `info` - Информационные сообщения
-   `debug` - Отладочная информация

### Структура логов

```json
{
	"level": "info",
	"time": "2024-01-01T00:00:00.000Z",
	"pid": 1234,
	"hostname": "server-1",
	"msg": "User registered",
	"userId": 123456,
	"username": "player1"
}
```

## 🧪 Тестирование

### Unit тесты

```bash
npm test
```

### Integration тесты

```bash
npm run test:integration
```

### Примеры тестовых данных

См. папку `examples/` для примеров запросов и ответов.

## 📊 Мониторинг

### Метрики

-   Количество активных пользователей
-   Время отклика API
-   Ошибки и исключения
-   Использование ресурсов

### Health Check

```
GET /health
```

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

MIT License

## 📞 Поддержка

-   Email: support@nebulahant.com
-   Telegram: @nebulahant_support
-   Issues: GitHub Issues

---

**Nebulahant Server** - Космическая стратегия в Telegram Mini Apps
