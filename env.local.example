# =============================================================================
# Nebulahunt Server - Local Environment Variables
# =============================================================================
# 
# ⚠️  ВАЖНО: Этот файл содержит локальные настройки, которые НЕ попадают в git.
# Скопируйте этот файл в .env.local и заполните вашими локальными значениями.
# Переменные из .env.local имеют приоритет над переменными из .env
#
# =============================================================================

# =============================================================================
# ЛОКАЛЬНЫЕ НАСТРОЙКИ РАЗРАБОТКИ
# =============================================================================

# Переопределение порта для локальной разработки
PORT=3001

# Локальные настройки базы данных (переопределяют .env)
DB_HOST_DEV=localhost
DB_PORT_DEV=5432
DB_NAME_DEV=nebulahunt_dev_local
DB_USER_DEV=postgres
DB_PASSWORD_DEV=your_local_password
DB_LOGGING=true

# Локальные JWT секреты (переопределяют .env)
JWT_ACCESS_SECRET=local_access_secret_key_change_this_in_production_64_chars_min
JWT_REFRESH_SECRET=local_refresh_secret_key_change_this_in_production_64_chars_min

# Локальный токен бота (переопределяет .env)
BOT_TOKEN=your_local_telegram_bot_token

# Локальные настройки безопасности
ADMIN_IDS=123456789
ADMIN_INIT_SECRET=local_admin_secret_change_this_in_production

# Локальные CORS настройки
CORS_ORIGIN=http://localhost:3000
CLIENT_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3002,http://localhost:3003

# Локальные настройки логирования
LOG_LEVEL=debug
LOG_FILE_PATH=logs/app-local.log

# Локальные настройки мониторинга
PROMETHEUS_PORT=9091
METRICS_ENABLED=true

# Локальные настройки внешних сервисов
TON_NETWORK=testnet
TON_API_KEY=your_local_ton_api_key
TON_WALLET_ADDRESS=your_local_ton_wallet_address

# Локальные настройки Redis (если используется локально)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_local_redis_password

# Локальные настройки миграций
RUN_MIGRATIONS=true

# =============================================================================
# ЛОКАЛЬНЫЕ НАСТРОЙКИ РАЗРАБОТКИ (ДОПОЛНИТЕЛЬНЫЕ)
# =============================================================================

# Отключение rate limiting для локальной разработки
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=10000

# Отключение IP ограничений для локальной разработки
ADMIN_IP_RESTRICTION=false

# Локальные настройки SSL (если нужно)
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=false

# Локальные настройки пула соединений
DB_POOL_MAX=10
DB_POOL_MIN=2
DB_POOL_ACQUIRE=30000
DB_POOL_IDLE=10000

# =============================================================================
# ЛОКАЛЬНЫЕ EMAIL НАСТРОЙКИ
# =============================================================================

# Email настройки для локальной разработки
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=test@ethereal.email
SMTP_PASS=test123
SMTP_SECURE=false
SMTP_FROM=noreply@nebulahunt.local
FRONTEND_URL=http://localhost:3000

# Ethereal Email для разработки (тестовый SMTP)
ETHEREAL_USER=test@ethereal.email
ETHEREAL_PASS=test123 